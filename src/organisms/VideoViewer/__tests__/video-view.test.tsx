import React from 'react';
import {render} from '@testing-library/react';
import VideoViewer from '..';

describe('<VideoViewer />', () => {
  test('render default video viewer', () => {
    // const {container} = render(
    //   // <VideoViewer/>
    // );

  });
});