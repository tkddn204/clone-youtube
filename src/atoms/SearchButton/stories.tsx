import React from 'react';
import SearchButton from '.';

export default {
  title: 'Atom/SearchButton',
  component: SearchButton,
};

export const DefaultSearchButton = () => <div>
  <SearchButton/>
</div>;