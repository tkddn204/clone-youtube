import React from 'react';
import SearchInput from '.';

export default {
  title: 'Atom/SearchInput',
  component: SearchInput,
};

export const DefaultSearchInput = () => <div>
  <SearchInput onChange={(e) => {
  }}/>
</div>;